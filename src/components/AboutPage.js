/* import React from 'react'
import Carousel from 'flat-carousel'

export const About = () => {

    const people = [
        {
            index: "1",
            name: "Andre",
            texto: "Texto aqui"
        },
        {
            index: "2",
            name: "Lucas",
            texto: "Texto aqui"
        }
    ]

    return (
        <Carousel>
        {   people.map((person) => {
                return (
                    <div key={person.index}> {person.name} </div>
                )
            })
        }
        </Carousel>
    )
}
 */